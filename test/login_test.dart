import 'package:flutter_test/flutter_test.dart';
// TODO: import 'package:framework_digital_ecommerce/usecases/';

void main(){
  test('Is valid email', () {
		//expect(true, validateEmail('test@gmail.com'));	
	});
	test('Is valid email', () {
		//expect(false, validateEmail('testgmail.com'));	
	});
	test('Is not harmful', () {
		//expect(false, validateEmail('@Process.run("explorer", "www.google.com")'));
		//expect(false, validateEmail('@Process.run("explorer", "www.google.com")'));
	});
}
